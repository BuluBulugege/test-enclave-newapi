# build outputs
/relay-core
/relay-verify
/relay-core-server
*.exe

# SGX build artifacts (regenerated by build_enclave.sh / Dockerfile.reproducible)
*.manifest
*.manifest.sgx
*.sig
/enclave/
secrets_enc/

# gramine signing key — NEVER commit (MRSIGNER only; users pin MRENCLAVE)
*.pem
enclave-key.pem

# databases / secrets / local
*.db
*.db-*
.env
.env.*
*.log
